// variables var let const
// variable is like container to store the value
// three diff way we can assign the value 
// var, let , const
// es6 = it's version this has own feture and it'll change the year waise 
// ecma script 
// js is top_down approch 

var name = "Manu".split('')
console.log(name);
console.log(typeof name);

var na = "mau"
console.log(na);
na = 'dd'
console.log(na);

// var we can reuse and reassing value 
// reinitialize
// variable cna star with letter , undersore ,  number, doller

var $name = "jhone"
console.log($name);

let vale = "tt"
console.log(vale);
vale = "lter"
console.log(vale);

// const
const va= "ram"
console.log(va);

// object when use the while create a array with type has const 
// u can reinitialize the are with perticular propertie

const person = {
    name : "ramkesh"
}
person.name = 'ram'
console.log(person.name);

const numbers = [1,2,3,4,5]
numbers.push(5+2)
console.log(numbers);









